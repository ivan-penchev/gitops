apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - cluster-issuer.yaml
  - cilium-lb.yaml
  # Cloudflare creds live in ONE SOPS file (two namespaced Secrets inside).
  # It lives in the controllers layer (with its cloudflared consumer + the
  # cert-manager namespace), decrypted by the controllers Kustomization.
