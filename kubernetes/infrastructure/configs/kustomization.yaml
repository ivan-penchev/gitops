apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - cluster-issuer.yaml
  - cilium-lb.yaml
  # Cloudflare creds live in ONE SOPS file (two namespaced Secrets inside).
  - cloudflare.sops.yaml   # cloudflared-token @ cloudflared, cloudflare-api-token @ cert-manager
